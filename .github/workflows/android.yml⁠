name: Build Android APK
on:
  push:
    branches: [ main, master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'zulu'
    - name: Initialize Cordova
      run: |
        npm install -g cordova
        cordova create build_dir uz.dilshodbek.taximeterpro "Taksometr Pro"
        cd build_dir
        rm -rf www/*
    - name: Copy files
      run: |
        cp -r index.html config.xml *.png build_dir/www/ || true
        cp config.xml build_dir/config.xml
    - name: Build APK
      run: |
        cd build_dir
        cordova platform add android
        cordova build android --device
    - name: Upload APK artifact
      uses: actions/upload-artifact@v3
      with:
        name: app-debug
        path: build_dir/platforms/android/app/build/outputs/apk/debug/app-debug.apk
